# mySQL-Compiler
Developed a custom compiler for a subset of the MySQL language using the tools Flex/Bison. Designed the BNF grammar and implemented it in a LALR parser that validates a SQL queries. That includes SELECT, CREATE and JOIN. Implemented syntax error handling with proper token recognition. Gained understanding about automata theory, and language design.
